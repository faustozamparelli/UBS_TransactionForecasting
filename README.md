# UBS transaction project

The transaction feature embedding is implemented in
[`transaction_embedding/`](transaction_embedding/README.md).

It converts each cleaned transaction row into a **128-number vector** for the
sequence model, while keeping raw data and generated embeddings out of Git.

